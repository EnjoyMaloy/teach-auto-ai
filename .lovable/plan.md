## План

В `src/pages/Auth.tsx` (правая часть страницы авторизации):

1. **Сделать контейнер видео квадратным (1:1):**
   - Внешний враппер (line 543) изменить так, чтобы внутри центрировался квадратный фрейм: `flex items-center justify-center`.
   - Градиентную рамку (line 544) сделать квадратной: `aspect-square w-full max-w-full max-h-full` (вместо `w-full h-full`), чтобы она занимала максимум по меньшей стороне и сохраняла 1:1.
   - Внутренний контейнер (line 545) — тоже `aspect-square w-full`, `overflow-hidden`, скругления `rounded-3xl` сохраняются.

2. **Видео без обрезки:**
   - У `<video>` (line 546) заменить `object-cover` на `object-contain`, оставить `absolute inset-0 w-full h-full`. Видео целиком помещается внутри квадрата без горизонтального кропа.

3. **Уменьшить текст баннера** "Turn ideas into interactive courses" (line 549):
   - Сейчас `text-3xl` → сделать `text-2xl` (≈ −25%), оставить `font-semibold leading-tight`.
   - Возможно немного подтянуть отступ снизу/сбоку (`bottom-8 left-8 right-8`), чтобы пропорции остались гармоничными к меньшему тексту.

4. Скругления, градиентная рамка-обводка, оверлей затемнения снизу — сохранить как есть.

Изменения только визуальные, без правок логики и без правок других файлов.